<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="google-signin-client_id" content="YOUR_CLIENT_ID.apps.googleusercontent.com">
    <title>Voyager - Start Your Perfect Trip</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
        }
        .container {
            display: flex;
            height: 100vh;
            align-items: center;
            justify-content: center;
            background-color: #f0f0f0;
        }
        .form-container, .image-container {
            width: 50%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .form-container {
            background-color: #ffffff;
            padding: 2rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
        }
        .form-container h1 {
            font-size: 2rem;
            margin-bottom: 1rem;
        }
        .form-container form {
            display: flex;
            flex-direction: column;
            width: 100%;
        }
        .form-container form input {
            margin-bottom: 1rem;
            padding: 0.5rem;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        .form-container form button {
            padding: 0.5rem;
            background-color: #3a60d3;
            color: #ffffff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .google-login-button {
            margin-bottom: 1rem;
            background-color: #4285F4; /* Google Blue */
            color: #ffffff;
            border: None;
            border-radius: 4px;
            padding: 0.5rem;
            font-size: 1rem;
            cursor: pointer;
            text-align: center;
            display: block;
            width: 50%;
        }
        .google-login-button:hover {
            background-color: #357ae8; /* Darker Google Blue */
        }
        .image-container {
            background-image: url('/mnt/data/image.png'); /* Image URL */
            background-size: cover;
            background-position: center;
            position: relative;
        }
        .image-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 15px; /* Rounded corners */
        }
        .overlay {
            position: absolute;
            top: 10%;
            left: 10%;
            background-color: rgba(0, 0, 0, 0.5);
            padding: 1rem;
            border-radius: 4px;
            color: #ffffff;
        }
        .account-action {
            text-align: center;
            margin-top: 1rem;
        }
        .account-action a {
            color: #2d6a4f;
            text-decoration: none;
        }.separator {
        display: flex;
        align-items: center; /* Vertically align items */
        }

        .line {
        flex: 1;
        height: 0.5px;
        background-color: black; /* Adjust color as needed */
        }

        .text {
        margin: 0 110px; /* Add space around the text */
        }


    </style>
    <!-- Include the Google Platform Library -->
    <script src="https://apis.google.com/js/platform.js" async defer></script>
    <script>
        function onSignIn(googleUser) {
            var profile = googleUser.getBasicProfile();
            console.log('ID: ' + profile.getId()); // Do not send to your backend! Use an ID token instead.
            console.log('Name: ' + profile.getName());
            console.log('Image URL: ' + profile.getImageUrl());
            console.log('Email: ' + profile.getEmail()); // This is null if the 'email' scope is not present.
        }

        function signOut() {
            var auth2 = gapi.auth2.getAuthInstance();
            auth2.signOut().then(function () {
                console.log('User signed out.');
            });
        }
    </script>
    <script>
        function signOut() {
          var auth2 = gapi.auth2.getAuthInstance();
          auth2.signOut().then(function () {
            console.log('User signed out.');
          });
        }
    </script>
</head>
<body>
    <div class="container">
        <div class="image-container">
            <img src="images/login-signup.jpeg" alt="Left Image">
        </div>
        <div class="form-container">
            <div>
                <h1>Here where your trip starts...</h1>
                
                <form action="#">
                    <input type="email" placeholder="Email" required>
                    <input type="password" placeholder="Password" required>
                    <button type="submit">Log In</button>
                </form><br>
                <div class="separator">
                    <span class="line"></span>
                    <span class="or">or</span>
                    <span class="line"></span>
                </div>
                <br>
                <button class="google-login-button">Log in with Google</button length >
                <div class="account-action">
                    <p>Don't have an account? <a href="#">Sign up</a></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
